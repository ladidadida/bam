name: pycas Integration Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  pycas-integration:
    name: Test with real pycas server
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Run pycas integration tests
      run: |
        cd tests/integration-pycas
        docker compose up --abort-on-container-exit --exit-code-from cascade-client
      
    - name: Clean up
      if: always()
      run: |
        cd tests/integration-pycas
        docker compose down -v
      
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pycas-integration-test-results
        path: tests/integration-pycas/*.xml
        retention-days: 7
